{
  "version": 2,
  "builds": [
    {
      "src": "dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js"
    }
  ],
  "installCommand": "npm i && npm run be:install && npm run fe:install",
  "buildCommand": "npm run build:dist && cd dist && npm install --production"
}